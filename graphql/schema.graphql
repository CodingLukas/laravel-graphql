type Query {
#    @paginate(defaultCount: 10)
#    User @find
    users: [User!]! @all
    user(id: ID! @eq): User @find
}

type User {
    id: ID!
    name: String!
    email: String!
    jobs: [Job!]! @hasMany
}

type Job {
    id: ID!
    title: String!

}
